# **📄 产品需求文档（PRD）：大宗商品与宏观经济高纯度数据采集管道**

| 属性 | 内容 |
| :---- | :---- |
| **文档版本** | v2.6 (Production Complete) |
| **产品名称** | 大宗商品与宏观经济高纯度数据采集管道 (Commodity Intel Pipeline) |
| **核心目标** | 打造具备防脏数据穿透、唯一性约束与时区感知的企业级高纯度 ETL 面板数据采集与智能推送引擎 |
| **数据下游** | 飞书多维表格（Fact Table）、飞书机器人（Interactive Card）、计量分析模型（Stata/Python Panel Data） |

## **一、 产品背景与业务痛点 (Background & Pain Points)**

在针对大宗商品基本面、地缘政治规制与宏观经济进行数据搜集与计量分析时，传统的自动化抓取脚本面临四大致命痛点：

> 1. **数据笛卡尔积膨胀（Data Explosion）**：以信息点为外层循环会导致同一篇新闻被不同的研究课题反复提取、多次写入，严重破坏面板数据（Panel Data）的样本唯一性。  
> 2. **泛新闻噪音穿透（Noise Contamination）**：RSS 源中混入了大量的体育赛事（如世界杯）、个人理财、公司八卦等噪音，强行写入会导致数据信噪比骤降。  
> 3. **外媒长句英文穿透**：当 LLM 解析波动或接口异常时，英文原文直接落库，影响多维表格的可读性与规范性。  
> 4. **网络 I/O 冗余与 API 限流（Rate Limiting）**：频繁、重复地向官方智库或媒体接口发起网络请求，极易触发 IP 封禁，破坏时间序列的连续性。

## **二、 系统架构与五重提纯漏斗 (System Architecture)**

系统抛弃了传统的“以课题/信息点为中心”的盲目轮询，全盘重构为“以文章/URL 为中心（Article-Centric）”的五重漏斗提纯管道：

\[源头 RSS Feeds (FT, EIA, Mining, Hellenic...)\]  
         │  
         ▼  【漏斗 1：内存级全局请求缓存 (Global Feed Cache)】  
\[解析条目 (Title, Link, Desc)\]  
         │  
         ▼  【漏斗 2：物理年份与 24h 时区窗口 (\_CUTOFF \= TODAY \- 1 day)】  
\[正文完整补全 (Trafilatura)\]  
         │  
         ▼  【漏斗 3：内存级局域打标与合并 (Core Matching & Merging)】  
\[大模型二分类与结构化提炼 (DeepSeek Gateway \+ 中文强制兜底)\]  
         │  
         ▼  【漏斗 4：黑白名单双重硬阻断 (Relevance Gate: is\_target\_related)】  
\[带指纹的结构化数据 JSON\]  
         │  
         ▼  【漏斗 5：写入前双重指纹校验 (Pre-Write Duplicate Guard)】  
\[飞书多维表格 (Feishu Fact Table) 唯一落库\]

## **三、 核心流转节点与技术规则 (Detailed Specifications)**

### **1\. 内存级全局请求缓存（Global Feed Cache）**

* **设计逻辑**：建立全局字典 \_RSS\_FEED\_CACHE。同一进程内，相同的 feed\_url 仅发起**唯一一次**网络 HTTP 请求。  
* **价值**：避免重复拉取同一官方源，降低 80% 以上的网络 I/O 消耗，彻底规避 API 限流与 IP 封禁风险。

### **2\. 时区感知与物理年份漏斗（Timezone-Aware & Year Filter）**

* **滑动窗口**：采用适应国际时差的 24 小时滑动窗口（\_CUTOFF \= TODAY \- 1 day），兼顾数据新鲜度与海外夜间新闻的召回率（官方月报保留 30 天宽窗口 \_CUTOFF\_30）。  
* **年份硬拦截**：在 RSS 解析层，直接通过正则 \_HIST\_YEAR\_RE 检验标题和 URL。若明确包含历史年份（如 2025、2024 等），在正文抓取前**物理丢弃**，不浪费后续算力。

### **3\. 相关性网关与二分类拦截器（Relevance Gateway）**

在发送给 DeepSeek 的 JSON Schema 中置入强制二分类与双重黑白名单约束：

* **7 类无条件黑名单**：体育赛事（世界杯/NBA 等）、个人理财、娱乐八卦、纯科技消费品发布、地方民生新闻、纯公司治理八卦、宗教文化旅游。  
* **6 类正向准入白名单**：大宗商品供需/价格/库存、宏观经济数据传导、地缘政治供应链冲击、航运运价与通道动态、监管政策（关税/储备/CBAM）、交易所期货结构。  
* **断路器逻辑**：若 is\_target\_related 为 false，LLM 必须填写 rejection\_reason 并将其他字段置为 null；代码层捕获后返回哨兵值 \_\_NOT\_RELEVANT\_\_，物理阻断落库。

### **4\. 强制中文与异常降维兜底（Mandatory Chinese Summary）**

* **Prompt 铁律**：强制要求 analytical\_summary 及所有输出字段 100% 使用专业简洁的中文表达，严禁保留英文原文长句。  
* **二次兜底机制（\_deepseek\_fallback\_zh）**：若 summarize\_v2 触发解析异常或提炼失败，系统启动二次极简请求，将英文内容压缩为 ≤150 字的中文商业摘要，保证落库文本 **100% 中文化**。

### **5\. 写入前双重指纹校验（Pre-Write Duplicate Guard）**

* **URL 精确指纹（existing\_urls）**：落库前调用 get\_today\_records 比对当日表格中的来源链接，若碰撞成功直接阻断。  
* **摘要内容指纹（existing\_fingerprints）**：比对摘要前 50 字的特征码。若不同媒体通稿报道同一核心事件，触发内容雷同阻断，实现系统**绝对幂等性（Idempotency）**。

## **四、 飞书多维表格业务数据字典 (Data Dictionary)**

彻底删除废弃字段，飞书多维表格事实表（Fact Table）的标准化字段映射如下：

| 字段名称 (Field Name) | 字段类型 (Type) | 约束条件 (Constraint) | 业务生成逻辑与学术/商业价值 (Extraction & Design Logic) |
| :---- | :---- | :---- | :---- |
| **采集时间** | 毫秒时间戳 | 必填 | 系统运行时间戳（TODAY\_MS），用于面板数据的时间序列对齐。 |
| **一级分类** | 单选/文本 | 必填 | 继承自课题配置的宏观领域分类（如：市场基本面、宏观政策）。 |
| **二级信息点** | 文本 | 必填 | 匹配到的 1\~N 个研究课题名称（多课题命中时自动逗号拼接，消除重复数据）。 |
| **研究核心** | 文本 | 可空 | 对应的课题研究核心定义前 300 字上下文。 |
| **标题** | 文本 | 必填 | LLM 提取的 商品品类/主题 \+ 最新动态（控制在 40 字以内）。 |
| **摘要** | 多行文本 | 必填 | **格式化高密数据洞察**：【报告时间】+ 商品品类 \+ 关键量化指标 \+ 期限结构/政策状态 \+ 100% 中文高浓度传导逻辑。 |
| **来源链接** | 链接 (URL) | **主键 (Primary Key)** | 全局绝对唯一，用于触发 Write-Guard 指纹去重校验，实现绝对幂等性。 |
| **来源网站名称** | 文本 | 必填 | 域名自动映射显示名称（如：Hellenic Shipping News, FT, EIA, Mining.com）。 |

## **五、 消息推送与交互卡片规范 (Daily Report & Card Schema)**

### **1\. 触发与调度机制 (Trigger & Schedule)**

* **静默触发时间**：每日清晨 **05:00**（Cron Schedule）。避开高频时段，为 24h 增量窗口的数据拉取、LLM 提炼与指纹去重预留缓冲。  
* **推送时间**：每日 **05:15 \~ 05:30**。数据采集与清洗完成且有新增写入记录（written \> 0）后，自动触发 reporter.py 向飞书群投递。  
* **空数据守卫（Skip Push）**：若当日无新增数据（written \== 0），系统触发 \[Skip Push\] 逻辑，静默终止推送，绝对不向用户投递空卡片。

### **2\. 飞书互动卡片结构规范 (Interactive Card Schema)**

卡片严格按以下层次进行渲染：

| 卡片模块 (Block) | 元素组件 (Component) | 内容规范与渲染逻辑 (Formatting & Schema Rules) |
| :---- | :---- | :---- |
| **Header 头部** | 蓝色背景 Header | 标题格式：🌐 大宗商品与宏观每日快报 · YYYY年MM月DD日  |
| **Top Dashboard** | AI 看点总览 | 1\. 统计行：📊 今日共采集 {total} 条高纯度洞察 2\. **AI 看点（Executive Summary）**：提取前 8 条记录的摘要，调用 DeepSeek 提炼生成 ≤100 字的 💡 今日核心看点  |
| **Grouped Items** | 分组精选展示 | 1\. **分类标题**：按 一级分类 分组（如：📈 市场基本面、🏛️ 宏观政策） 2\. **展示上限**：**每个分类最多精选展示 3 条** 3\. **单条明细格式**： 📌 \[{level2}\] {title} • 摘要：{short\_summary} 🔗 \[查看源网页\]({source\_url}) 4\. **折叠提示**：若某分类 \> 3 条，末尾标注 \*…还有 {N} 条高价值数据，详见多维表格\*  |
| **Footer Action** | 主行动点按钮 | 放置跳转 Button 查看飞书多维完整面板，点击可直接拉起浏览器并精准跳转至 Bitable 多维表格页面。 |

## **六、 验收测试结果与量化表现 (Acceptance Metrics)**

根据实际测试跑批验证，本管道展现出如下工程指标：

> 1. **去重与降维效果**：原本 130 条重复写入的数据物理收敛为 48 条绝对独立的高价值信息，去重率达 **63%**，彻底解决样本重叠问题。  
> 2. **信噪比与防穿透表现**：100% 成功阻断“世界杯财务”、“前学生免费理发”等非目标领域新闻。  
> 3. **幂等性验证**：多次重复触发 fetch\_data.py，后续运行触发 \[Write-Guard\] 全量阻断，新增写入数为 0，系统稳定性达到生产级要求。  
> 4. **语言纯净度**：跨国 RSS 数据入库摘要 100% 实现专业中文转化，长句英文穿透率降为 **0%**。